# HopStat v2.1.65

Fix BGP route table Age column clipping long uptime values.

## Fixes

- **Route Age column** — widen the desktop BGP results table Age column so values like `13h52m54s` display fully without being cut off.
- **Age cell typography** — use tabular numerals for cleaner alignment of route age values.
