# HopStat v2.1.68

Smaller typography on the admin dashboard version card to match system resource status text.

## What's New

- **Version card** — current version, status badge (e.g. Up to date), and hint text now use the same compact size as CPU resource status labels on the dashboard.
