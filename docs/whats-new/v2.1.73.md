# HopStat v2.1.73

Language switcher replaced with a compact select dropdown in both the main site header and admin panel.

## What's New

- **Language select** — the language toggle buttons are replaced by a native `<select>` dropdown; the active locale is shown as the selected option, saving horizontal space in both the header and admin toolbar.
