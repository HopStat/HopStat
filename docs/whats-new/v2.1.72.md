# HopStat v2.1.72

Internal quality improvements: fixed data-race conditions detected by the Go race detector and achieved 100% test coverage across all packages.

## What's New

- **Race condition fixes** — eliminated three data races in BGP peer-watch injection, circuit-breaker probe sequencing, and command-stream buffering, ensuring clean runs under `go test -race`.
- **100% test coverage** — removed unreachable dead-code branches and added targeted tests to close all remaining coverage gaps.
